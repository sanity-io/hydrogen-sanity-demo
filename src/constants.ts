import clsx from 'clsx';

export const DEFAULT_BUTTON_STYLES = clsx([
  'flex h-[2.5rem] duration-200 ease-out transition-all items-center justify-center overflow-hidden rounded-full bg-offBlack p-4 text-sm font-bold text-white disabled:opacity-20',
  'hover:opacity-90',
]);

export const COLLECTION_PAGE_SIZE = 8;
